name: Deploy Git.WTF

on: 
  push:
  pull_request:
    branches: 
      - master

jobs:
  deploy:
    name: deploy to production
    runs-on: ubuntu-18.04
    env:
      DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
      DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
    steps:
    - uses: actions/checkout@v2
    - name: setup ssh session 1
      run: |
        eval "$(ssh-agent -s)"
        echo ${SSH_PASS} | ssh-add - <<< "${DEPLOY_KEY}"
    - name: setup ssh session 3
      run: mkdir ~/.ssh
    - name: setup ssh session 4
      run: echo -e "HostName *\n\tStrictHostKeyChecking no" >> ~/.ssh/config
    - name: login and test ssh access
      run: ssh ${DEPLOY_USER}@${DEPLOY_HOST} 'cd ~/git.wtf && pwd'
    - name: login and run deploy script
      run: ssh ${DEPLOY_USER}@${DEPLOY_HOST} 'bash -s' < deploy.sh

